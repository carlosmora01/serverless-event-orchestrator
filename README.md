# serverless-event-orchestrator
Generic event router for AWS Lambda with support for segmented routing (public, private, backoffice), Cognito User Pool validation, and built-in infrastructure middlewares like CORS and JSON parsing.
